{
  "name": "github-folder-dl-ext",
  "version": "1.0.0",
  "description": "Chrome extension to download GitHub repo folders as file structures",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "test": "node test/cdp/harness.js",
    "test:unit": "node --test test/unit/"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "puppeteer": "^23.0.0"
  }
}
